{% extends 'base.html.twig' %}

{% block title %}Home page{% endblock %}

{% block body %}
    <h1>Home page</h1>
    <a href="{{ path("app_login") }}">Login</a>
    <a href="{{ path("app_register") }}">Registrer</a>
    <a href="{{ path("app_logout") }}">Logout</a>
{% endblock %}
